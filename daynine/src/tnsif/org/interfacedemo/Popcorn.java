
package tnsif.org.interfacedemo;

/*an interface which contains exactly one abstract method is called as functional interface
 * below annotation ensures that we have to use exactly one abstract method inside the interface*/
@FunctiionalInterface
public interface Popcorn {
	void displayReciepe();
}
